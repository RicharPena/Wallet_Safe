import 'package:flutter/material.dart';

class EstadisticasTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Estadísticas')),
      body: Center(child: Text('Gráficos y estadísticas')),
    );
  }
}
