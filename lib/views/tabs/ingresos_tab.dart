import 'package:flutter/material.dart';

class IngresosTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Agregar Ingresos')),
      body: Center(child: Text('Aquí van los ingresos')),
    );
  }
}
