import 'package:flutter/material.dart';

class ConfiguracionTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Configuración')),
      body: Center(child: Text('Opciones de la cuenta')),
    );
  }
}
